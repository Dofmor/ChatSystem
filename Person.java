public class Person {
	protected String name;
	protected String username;
	protected String password;
	protected String userType;
	
	public void login(String username, String password, String userType) {
		//Calls user authentication from the server with the passed login variables
		//userAuthentication(username, password, userType);
	}
	
	public void sendMessage(String dataType, String data, String fromClient, String toClient, String fromServer, String toServer) {
		//Sends messages with the entered in info
		//Creats new message object
		//Message message = new Message();
		//message.type = dataType;
		//message.data = data;
		//message.fromClient = fromClient;
		//message.toClient = toClient;
		//message.fromServer = fromServer;
		//message.toServer = toServer;
		//sendCoversation(message);
	}
}
